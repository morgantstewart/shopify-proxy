```json
{
  "name": "shopify-proxy",
  "version": "1.0.0",
  "description": "Shopify proxy for Framer integration",
  "scripts": {
    "dev": "vercel dev"
  }
}
```

---

## File 3: `vercel.json`

Create this file at: `vercel.json` (root of project)

```json
{
  "functions": {
    "api/shopify/product.ts": {
      "runtime": "nodejs18.x"
    }
  }
}
```
